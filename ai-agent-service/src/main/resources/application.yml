server:
  port: ${SERVER_PORT:8086}

spring:
  application:
    name: ai-agent-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
