server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: hotel-search-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
